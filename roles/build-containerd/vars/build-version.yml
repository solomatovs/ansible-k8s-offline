local_src_dir: "{{ artifact_dir_src_local }}/containerd-{{ build_version.version }}"
local_bin_dir: "{{ artifact_dir_bin_local }}/containerd-{{ build_version.version }}"

remote_src_dir: "{{ artifact_dir_src_remote }}/containerd-{{ build_version.version }}"
remote_bin_dir: "{{ artifact_dir_bin_remote }}/containerd-{{ build_version.version }}"

archive_src: "containerd-{{ build_version.version }}.tar.gz"
archive_bin: "containerd-{{ build_version.version }}.tar.gz"

docker_container_name: "containerd-{{ build_version.version }}"
docker_image_name: "containerd"
docker_image_tag: "{{ build_version.version }}"
