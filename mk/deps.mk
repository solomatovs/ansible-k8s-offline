# =============================================================================
# Макросы для работы с зависимостями
# Подключение: include ../mk/deps.mk
# =============================================================================

# Проверка наличия dep-файла
# Использование: @$(foreach d,$(_ALL_DEP_FILES),$(call check_dep,$(d)))
check_dep = test -f $(1) || { echo "Ошибка: $(1) не найден" >&2; echo "Выполните: make deps $(V)" >&2; exit 1; };
